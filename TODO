# TODO


- [x] Implement moderation on/off toggle in the UI and request pipeline.
- [x] Add support for configurable moderation levels/categories per model (when supported).
- [x] Implement document upload for PDF, CSV, and Word files and attach to prompts.
- [x] Build a chat-like response view that includes any moderation responses.
- [x] Show a loading spinner while waiting for LLM responses.
- [x] Provide provider selection for OpenAI, and Gemini.
- [x] When the user has selected OpenAI as a provider, they can select the models o3, o4-mini, gpt-5 gpt-5.1 and gpt-5.2
- [x] When moderation is on for OpenAI, the request is sent to the moderation endpoint, then to the selected model. 
- [x] When moderation is off for OpenAI the request is sent only to the selected model. 
- [x] When the user has selected Gemini, they can select gemini-2.5-pro, gemini-2.5-flash
- [x] When the user has selected Gemini with moderation on, then the safety settings are set to high for all categories
- [x] When the user has selected Gemini with moderation off, then the safety settings are set to negligible for all categories
- [x] Implement provider integrations in Python with API key configuration.
- [x] Deliver a local web UI accessible via browser.
- [x] Implement more logging to the terminal so I can see what's happening.
- [x] Improve the loading spinner.
- [x] Handle formatting of the output (currently unformatted JSON).
- [x] Update Gemini model IDs in the UI to current values (gemini-2.5-pro, gemini-2.5-flash).
